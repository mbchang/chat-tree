// Animation settings for node zoom
export const ZOOM_ANIMATION = {
  targetZoom: 1.5,
  steps: 20,
  stepDuration: 20, // ms
  transitionDuration: 50, // ms
} as const;

// Node dimensions
export const NODE_WIDTH = 600;

// Handle styles
export const HANDLE_STYLE = {
  background: '#555',
} as const;

